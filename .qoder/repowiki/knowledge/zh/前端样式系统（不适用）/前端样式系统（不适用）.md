---
kind: frontend_style
name: 前端样式系统（不适用）
category: frontend_style
scope:
    - '**'
---

该仓库是一个纯 CUDA/C++ 数值计算基准测试项目，仅包含 `gemm_benchmark.cu`、`gemm_benchmark/` 目录和 `Makefile`，没有任何 CSS、SCSS、HTML、JavaScript 或前端框架相关代码。因此本仓库不存在前端样式系统，`frontend_style` 类别不适用于此项目。