需要 NVIDIA A100 GPU 及匹配的 CUDA Toolkit（CUDA_PATH 默认 /usr/local/cuda）。常用命令：make（编译）、make run（默认 2048/4096/8192 三组测试）、make quick（仅 2048×2048 快速验证）、make large（含 16384 大规模测试）、./gemm_benchmark <N> <iters> 自定义参数。